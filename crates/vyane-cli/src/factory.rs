//! Re-export of the service-layer executor factory.
//!
//! The config loading, runtime assembly, and executor factory now live in
//! `vyane-service`. This re-export is kept for any CLI code that still
//! references the factory module path directly.
