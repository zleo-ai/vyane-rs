# WP-124 — multi-thread remaining authorized OpenAI Chat unit fixtures

## Outcome

Run the remaining current-thread authorized OpenAI Chat MockServer unit fixtures
on a two-worker Tokio runtime so the full suite uses the same scheduling class.

Production protocol client behavior does not change.

## Motivation

WP-117 multi-threaded dual-mock redirect and cancellation fixtures. Six other
MockServer-backed cases in the same file still used the default current-thread
runtime.

## Acceptance

1. All authorized OpenAI Chat unit fixtures use two Tokio workers.
2. Existing assertions remain unchanged.
3. No production code changes.
4. Local targeted runs, gates, review, PR CI, and exact merged-main CI pass.
