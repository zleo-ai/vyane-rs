# WP-119 — isolate mixed process/native concurrent lane fixture in CI

## Outcome

Run `mixed_process_and_native_lanes_run_concurrently_in_one_daemon` in a fresh
CI process for normal and coverage jobs, before the long dual-path workflow
isolation and alongside the process-lane success isolation from WP-118.

Production multi-lane AgentRun behavior does not change.

## Failure evidence

Main run `30535000154` at `dcbe58b` failed Ubuntu on
`mixed_process_and_native_lanes_run_concurrently_in_one_daemon` with
`AgentRun submission did not return a response: total retry budget expired`
and only the daemon listen line. macOS and coverage passed. Process-agent
isolation alone (WP-118) was not enough when this concurrent fixture stayed
inside the serialized suite.

## Acceptance

1. Normal and coverage jobs skip the mixed-lanes fixture in the serialized suite.
2. Both jobs re-run it alone via `--exact` before dual-path workflow isolation.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
