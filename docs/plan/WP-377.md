# WP-377 — Wire pure AgentStoreError line on process AgentRun create

## Outcome

Daemon Process AgentRun create failures that cannot exact-retry log pure
`format_agent_store_error_kind_line`, matching the native create wire from WP-376.

## Motivation

WP-376 closed the native create residual. Process harness create still discarded
the store error as `Err(_)` and returned conflict without a kind diagnostic.

## Acceptance

1. Non-retryable process create Err logs pure agent-store line + `error = as_str`.
2. Exact-retry success path stays silent on this branch.
3. Local gates and independent review pass.
