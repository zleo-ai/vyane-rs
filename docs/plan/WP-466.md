# WP-466 — Isolate uncertain native recovery fixture

## Outcome

Run
`agent_supervisor::tests::uncertain_execution_is_recovered_without_automatic_resume`
in its own test process after the serialized `vyane-service` suite.

Production supervisor, recovery, lease and timeout behavior do not change.

## Failure evidence

Exact-main CI run `30929105780` at
`86eb3949ace8b510824399a0db8e37803df6db8a` completed the Ubuntu test job and
all other required Linux jobs. The macOS test job reached the serialized
`vyane-service` suite, completed the preceding supervisor fixture, then made
no further progress until the 30-minute job limit cancelled it. Libtest order
places the uncertain-recovery fixture next; the already-isolated resident
completion fixture was skipped in that process.

The uncertain-recovery fixture passes repeatedly in a fresh process. This is
therefore the same test-runtime handoff class already documented for adjacent
resident service lifecycle fixtures, not evidence that production recovery
needs a larger lease or timeout.

## Acceptance

1. The main serialized `vyane-service` invocation skips the uncertain-recovery
   fixture.
2. A following command runs that fixture by its exact name in a fresh process.
3. Assertions and all production lease, recovery and timeout budgets remain
   unchanged.
4. The exact fixture passes repeatedly, the complete service CI command
   sequence passes locally, and workspace gates remain green.
5. The exact head receives independent review and required GitHub checks
   before merge.
