# WP-98 — reconcile public daemon lifecycle status

## 目标

修正 README 中已经过期的 daemon Native graceful-shutdown 状态，使公开说明
与 WP-66、roadmap 和现行接受测试一致。

本包只对齐文档，不改变产品或 CI 行为。

## 状态依据

- WP-66 已覆盖 Native cooperative cancellation、deadline-bound restart
  settlement、no replay、no output 和 terminal residue cleanup。
- WP-95 保持启动前严格检查，并让启动后的类型化控制锁竞争在总预算内重试。
- WP-96 修复强杀重启 fixture 的退出等待，不放宽生产启动检查。
- Native resume、live pause/resume 和 automatic replay 仍明确未实现。

## 验收门

1. README 不再把已经完成的 Native graceful-shutdown race acceptance 标为
   open。
2. 已完成范围和仍开放范围都与 roadmap 一致。
3. 文档链接、format、Clippy、全仓测试与 parity 通过。
