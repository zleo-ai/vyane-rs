# WP-118 — isolate process-lane AgentRun success fixture in CI

## Outcome

Run `process_agent_success_is_idempotent_and_publishes_exact_output` in a fresh
CI process after the rest of the CLI suite, for both normal and coverage jobs.

Production Process AgentRun behavior does not change. Fixture budgets and
assertions stay the same.

## Failure evidence

PR #69 Ubuntu run `30531912723` failed
`process_agent_success_is_idempotent_and_publishes_exact_output` with
`AgentRun submission did not return a response: total retry budget expired`
while the daemon log only showed the listen line. macOS and coverage on the
same SHA passed; a failed-job re-run then passed. The body is stable alone;
suite handoff after earlier multi-thread daemon fixtures can leave the first
submit HTTP accept starved.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
