# WP-448 — Pure GoalNextActionKind on REST continuity-next success

## Outcome

REST `GET /v1/goals/{id}/continuity-next` logs pure
`format_goal_next_action_kind_line` for the projected allowlisted
`GoalNextActionKind` on success.

## Motivation

WP-406 covers CLI next-action kind. WP-447 covers REST accepted signals.
REST success already materializes closed `GoalNextActionView.action` without a
pure kind diagnostic (error path pure residual was WP-277).

## Acceptance

1. Add pure `format_goal_next_action_kind_line` over `GoalNextActionKind::as_str`.
2. REST success path logs pure line + `action = as_str` for the projected action.
3. Unit tests lock representative closed action tokens.
4. JSON response body and status mapping unchanged.
5. Local gates and independent review pass.
