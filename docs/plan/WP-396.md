# WP-396 — Pure GoalStatus on CLI goal human projection

## Outcome

Goal CLI human projection (`print_goal_line`) logs pure `format_goal_status_line`
for the goal's closed status.

## Motivation

WP-355/362 published and wired pure goal status on daemon settle. CLI human
goal prints still lacked a pure kind diagnostic on the projection path.

## Acceptance

1. `print_goal_line` logs pure goal-status line + structured `status = as_str`.
2. Human projection body remains unchanged.
3. Local gates and independent review pass.
