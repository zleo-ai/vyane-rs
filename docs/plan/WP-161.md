# WP-161 — isolate legacy job-reader drift fixture in CI

## Outcome

Run `legacy_job_reader_preserves_target_snapshot_drift_check` in a fresh CI
process for normal and coverage jobs.

Production legacy job-reader target-snapshot drift behavior does not change.

## Motivation

This multi_thread detach fixture preserves target-snapshot drift checks for the
legacy job reader. Neighboring multi_thread detach fixtures already required
process isolation under suite/GHA load.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
