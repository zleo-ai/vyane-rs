# WP-202 — CLI human cancel "already {state}" pure formatter + tests

## Outcome

Legacy and durable cancel idempotency human lines go through pure
`format_task_already_state_line` (`{id} already {state}`, terminal-safe id).

## Motivation

WP-200 covered cancel final-state settle lines; three "already terminal"
idempotency paths still used ad-hoc `println!` without escape coverage.

## Acceptance

1. Legacy cancel (terminal status), durable `report_cancel_interruption`, and
   `finish_observed_terminal_cancel` human paths use
   `format_task_already_state_line` (error paths unchanged).
2. Line shape: safe id, literal ` already `, Display/as_str state.
3. Unit tests drive the shipped formatter (escape + exact Display layout).
4. Local gates and independent review pass; no CI isolation train.
