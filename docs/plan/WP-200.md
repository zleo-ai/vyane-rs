# WP-200 — CLI human cancel final-state pure formatters + tests

## Outcome

Detached task cancel and daemon workflow cancel human one-liners go through
pure `format_task_final_state_line` and `format_workflow_cancel_line`
(terminal-safe ids).

## Motivation

Cancel settle paths still used ad-hoc `println!("{} {}", id, state)` without a
pure helper or escape coverage.

## Acceptance

1. Task cancel finalize and workflow cancel human paths use the pure formatters
   (JSON unchanged).
2. Task line: safe id + safe state; workflow line: `workflow {id} {state}`.
3. Unit tests drive the shipped formatters.
4. Local gates and independent review pass; no CI isolation train.
