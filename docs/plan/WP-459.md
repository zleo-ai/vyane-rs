# WP-459 — Pure TakeoverSandbox on continuity-execute dispatch freeze

## Outcome

CLI `goal continuity-execute` logs pure `format_takeover_sandbox_line` for the
frozen approval sandbox immediately before dispatch.

## Motivation

WP-410 covers queue-time sandbox freeze. Execute reuses the same closed
`TakeoverSandbox` on the dispatch path without pure-logging that frozen kind
(admission/consume/finish pure residual was WP-457/458).

## Acceptance

1. Reuse pure `format_takeover_sandbox_line` (no new private table).
2. After consume pure log, log pure sandbox line + `sandbox = as_str` before
   `dispatch`.
3. Unit tests lock Write/Full tokens (ReadOnly already locked).
4. Dispatch behavior and settle bodies unchanged.
5. Local gates and independent review pass.
