# WP-129 — isolate plain-to-auto-pursuit daemon start fixture in CI

## Outcome

Run
`enabling_auto_pursuit_requires_stopping_a_plain_existing_daemon_first` in a
fresh CI process for normal and coverage jobs.

Production daemon start / auto-pursuit admission behavior does not change.

## Failure evidence

Main `4cb3c5f` attempt 1 macOS failed this fixture with
`assertion failed: daemon.stop().status.success()` inside
`daemon_goal_acceptance`. Coverage and the full-workflow re-run succeeded. The
body is stable alone; suite handoff can leave the plain daemon stop unclean.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
