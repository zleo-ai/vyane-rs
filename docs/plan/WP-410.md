# WP-410 — Pure TakeoverSandbox on continuity queue

## Outcome

CLI `goal continuity-queue` logs pure `format_takeover_sandbox_line` with
structured `sandbox = as_str` for the frozen takeover approval sandbox after a
successful queue.

## Motivation

Takeover approvals freeze a closed `TakeoverSandbox`. Queue is the shipped path
that first persists that bound; pure kind diagnostics were missing.

## Acceptance

1. Add pure `format_takeover_sandbox_line` using `sandbox.as_str()`.
2. After successful `queue_takeover_approval`, log pure sandbox line + `sandbox = as_str`.
3. Unit test asserts stable token text.
4. JSON/human bodies unchanged.
5. Local gates and independent review pass.
