# WP-252 — Domain tokens for GoalContinuityRunStatus

## Outcome

`GoalContinuityRunStatus` exposes public `as_str` / `Display`.

## Motivation

The bounded continuity runner already serializes this closed redacted status
with serde snake_case. Domain tokens continue the goal continuity view train
after WP-251.

## Acceptance

1. `as_str` + `Display` for every variant.
2. Unit test locks multi-word tokens (`queue_applied`, `manual_decision_required`,
   `authority_unavailable`, `execution_blocked`, `timed_out`).
3. Local gates and independent review pass; no CI isolation train.
