# WP-159 — isolate detach bad-label fixture in CI

## Outcome

Run `detach_bad_label_exits_two_and_creates_no_task_dir` in a fresh CI process
for normal and coverage jobs.

Production detach bad-label early-exit behavior does not change.

## Motivation

This multi_thread detach fixture exercises label validation before task/process
creation. Neighboring multi_thread detach fixtures already required process
isolation under suite/GHA load.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
