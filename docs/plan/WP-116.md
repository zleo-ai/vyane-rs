# WP-116 — keep coverage dual-path workflow fixture schedulable

## Outcome

1. Keep `submitted_workflow_outlives_cli_and_explicit_id_retry_is_idempotent`
   independently schedulable under llvm-cov after the full instrumented CLI
   suite: more Tokio workers, non-blocking CLI start/submit/retry/stop, a
   hold-until-release mock gate (no wall-clock delay assumption) that proves
   admission-before-completion, and richer daemon-log panics on settlement
   timeout.
2. Drop llvm-cov incremental debris before the isolated coverage process for
   that fixture so disk/fd pressure from the preceding suite cannot starve the
   held mock path.

Production daemon, workflow, and timeout behavior do not change. Assertions
and the 120s terminal poll budget stay the same.

## Failure evidence

Main run `30528202959` at `68dc35d` (WP-115) failed coverage only:
`submitted_workflow_outlives_cli_and_explicit_id_retry_is_idempotent` stayed
`running` for the full 120s budget even though WP-112 already isolates the
fixture in a fresh process, uses two Tokio workers, and async status polling.
Ubuntu/macOS non-coverage jobs on the same SHA passed.

## Acceptance

1. Dual-path fixture uses four Tokio workers and never blocks them on CLI I/O.
2. Mock holds the target open until after a non-terminal submit is observed
   (no fixed delay race under llvm-cov).
3. Settlement timeouts include a daemon-log excerpt; the fixture still asserts
   exactly one mock hit after terminal completion.
4. Coverage job clears llvm-cov incremental dirs before the isolated process.
5. Local targeted runs, gates, review, PR CI, and exact merged-main CI pass.
