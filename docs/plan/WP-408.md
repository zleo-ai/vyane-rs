# WP-408 — Pure GoalContinuitySignalKind on continuity signal

## Outcome

CLI `goal continuity-signal` logs pure
`format_goal_continuity_signal_kind_line` with structured `kind = as_str`
after a successful durable signal record.

## Motivation

Continuity signal settle already freezes a closed `GoalContinuitySignalKind`.
WP-407 covered goal events; the signal kind on the shipped signal path still
lacked a pure kind diagnostic.

## Acceptance

1. Add pure `format_goal_continuity_signal_kind_line` using `kind.as_str()`.
2. `continuity_signal` logs pure signal-kind line + `kind = as_str` after record.
3. Unit test asserts stable token text.
4. JSON/human bodies unchanged.
5. Local gates and independent review pass.
