# WP-446 — Pure GoalContinuitySignalKind on continuity-next accepted signals

## Outcome

CLI `goal continuity-next` logs pure `format_goal_continuity_signal_kind_line`
for each projected `accepted_signals` kind (closed `GoalContinuitySignalKind`).

## Motivation

WP-408 covers continuity-signal settle. Continuity-next already materializes
`GoalContinuityNextAction.accepted_signals` on the shipped projection path but
did not pure-log those closed kinds (action/command/status/mode already pure).

## Acceptance

1. Reuse pure `format_goal_continuity_signal_kind_line` (no new private table).
2. `continuity_next` logs pure line + `kind = as_str` for each accepted signal.
3. Empty `accepted_signals` emits no extra signal pure logs.
4. Unit tests lock all three closed signal tokens.
5. JSON/human bodies unchanged.
6. Local gates and independent review pass.
