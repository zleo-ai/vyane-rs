# WP-364 — Clear dead_code residual on wired store/workflow pure lines

## Outcome

Remove leftover `#[allow(dead_code)]` from pure error formatters that WP-363
already wired onto daemon/MCP log paths.

## Motivation

Independent review of WP-363 noted three live formatters still carried
stale `dead_code` allows after call-site wires landed.

## Acceptance

1. `format_goal_store_error_kind_line` has no `#[allow(dead_code)]`.
2. `format_workflow_error_kind_line` has no `#[allow(dead_code)]`.
3. `format_task_store_error_kind_line` has no `#[allow(dead_code)]`.
4. Local gates and independent review pass.
