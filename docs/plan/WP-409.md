# WP-409 — Pure GoalContinuityOperatorCommand on continuity next

## Outcome

CLI `goal continuity-next` logs pure
`format_goal_continuity_operator_command_line` with structured
`command = as_str` when the projected next action carries an operator command.

## Motivation

WP-406 wired pure next-action kind. Optional operator command tokens already
appear in human columns via `as_str` but lacked a pure kind diagnostic.

## Acceptance

1. Add pure `format_goal_continuity_operator_command_line` using `command.as_str()`.
2. When `next_action.command` is Some, log pure command line + `command = as_str`.
3. Unit test asserts stable token text.
4. JSON/human bodies unchanged.
5. Local gates and independent review pass.
