# WP-248 — Domain tokens for RouteSelectionBasis

## Outcome

`RouteSelectionBasis` exposes public `as_str` / `Display`.

## Motivation

Route preview diagnostics already carry this closed enum. Domain tokens complete
the diagnostic enum token train (WP-245).

## Acceptance

1. `as_str` + `Display` for Preference/Default.
2. Unit test locks tokens.
3. Local gates and independent review pass; no CI isolation train.
