# WP-133 — isolate supervisor panic-backoff fixture in CI

## Outcome

Run `panics_use_bounded_exponential_backoff` in a fresh CI process for normal
and coverage jobs, alongside the existing `maintenance_runs_without_delivery_lanes`
isolation. Coverage also exact-runs the maintenance fixture for symmetry.

Production broker supervisor backoff behavior does not change.

## Failure evidence

PR #86 macOS cancelled after ~17 minutes with orphan process
`supervisor_cont` while `supervisor_contract` was mid-suite after
`lane_and_projector_failures_do_not_stop_unrelated_loops`. The next remaining
fixture is the paused-time panic backoff case. A failed-job re-run passed.

## Acceptance

1. Normal and coverage jobs skip the panic-backoff fixture in the serialized
   broker suite and re-run it alone via `--exact`.
2. Coverage also process-isolates the maintenance fixture like the normal job.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
