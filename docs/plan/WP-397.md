# WP-397 — Pure TakeoverApprovalStatus on takeover result projection

## Outcome

Goal takeover result printing logs pure `format_takeover_approval_status_line`,
and the human tab line uses `status.as_str()` instead of Display.

## Motivation

WP-393 wired pure status on decide. Shared `print_takeover_result` still used
Display for human columns without a pure kind log on all settle paths.

## Acceptance

1. `print_takeover_result` logs pure takeover-approval status line + `status = as_str`.
2. Human tab column uses `status.as_str()` (kind-only token).
3. Local gates and independent review pass.
