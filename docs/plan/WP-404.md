# WP-404 — Pure PursuitSegmentStatus on segment runtime

## Outcome

Shared `DispatchGoalRuntime::run_segment` logs pure
`format_pursuit_segment_status_line` with structured `status = as_str` for every
segment settle (CLI pursue and resident daemon).

## Motivation

WP-358 published pure pursuit segment status lines without a live host. The
shared segment adapter already maps `RunStatus` → `PursuitSegmentStatus` and is
the single production settle seam for both manual and daemon pursuit.

## Acceptance

1. After mapping segment outcome, log pure pursuit-segment status line + `status = as_str`.
2. Clear `#[allow(dead_code)]` on `format_pursuit_segment_status_line`.
3. Segment result values and exit behavior unchanged.
4. Local gates and independent review pass.
