# WP-128 — isolate expired-controller restart settlement fixture in CI

## Outcome

Run
`restart_settles_expired_native_controller_without_replaying_private_input` in a
fresh CI process for normal and coverage jobs, alongside the other daemon
AgentRun isolations.

Production restart settlement and no-replay guarantees do not change. The
fixture stays on the intentional current-thread Tokio runtime.

## Failure evidence

Local serialized `daemon_agent_acceptance` failed this fixture with
`left: "running"` / `right: "timed_out"` after suite neighbors, while three solo
re-runs passed (~4s each). Settlement after kill/restart can still observe a
non-terminal state under suite load even though the body is stable alone.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes; runtime flavor remains current-thread.
4. Local gates, review, PR CI, and exact merged-main CI pass.
