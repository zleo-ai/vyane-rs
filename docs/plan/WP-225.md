# WP-225 — Pure `vyane check` section header lines

## Outcome

Human `vyane check` section banners (`providers:`, `profiles:`, `harnesses:`,
`profile environment:`) go through pure helpers used by the shipped command.

## Motivation

WP-201 purified per-row check diagnostics; section headers remained ad-hoc
`println!` literals without unit contracts.

## Acceptance

1. Four section headers use pure format helpers (exact prior strings).
2. `run_check` wires those helpers (row formatters unchanged).
3. Unit tests drive the shipped helpers.
4. Local gates and independent review pass; no CI isolation train.
