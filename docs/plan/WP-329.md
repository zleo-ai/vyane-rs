# WP-329 — Pure observation kind + native session state lines

## Outcome

CLI ships pure `format_goal_observation_kind_line` and
`format_native_session_state_line` over WP-328 domain tokens.

## Motivation

WP-328 published observation and session-state kinds. Operator pure lines still
lacked formatters for those closed surfaces.

## Acceptance

1. Pure lines render `goal observation: <kind>` and `native session: <kind>`.
2. Unit tests lock payload-bearing variants without embedding secrets.
3. Local gates and independent review pass.
