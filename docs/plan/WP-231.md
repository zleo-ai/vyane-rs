# WP-231 — Display for TakeoverRunStatus

## Outcome

`TakeoverRunStatus` implements `Display` via its existing public `as_str`, matching
other takeover/goal domain enums.

## Motivation

WP-229–230 finished domain token APIs for most goal enums. `TakeoverRunStatus`
already had `as_str` (and is used in human approval evidence) but lacked
`Display`.

## Acceptance

1. `impl Display for TakeoverRunStatus` delegates to `as_str`.
2. Unit test locks Display tokens.
3. Local gates and independent review pass; no CI isolation train.
