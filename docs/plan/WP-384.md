# WP-384 — Pure task settlement on workflow finish error/join

## Outcome

Daemon workflow worker finish error and non-shutdown join paths log pure
`format_task_settlement_line` for the settled failure disposition.

## Motivation

WP-383 covered successful engine outcomes. Error and join settle paths still
produced `TaskSettlement::Failed` without pure settlement lines.

## Acceptance

1. Workflow engine Err settle logs pure task settlement line (+ existing workflow error line).
2. Non-shutdown join settle logs pure task settlement line with structured `error = "join"`.
3. Shutdown join interrupt remains body-free `error = "join"` without inventing a settlement.
4. Local gates and independent review pass.
