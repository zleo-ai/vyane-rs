# WP-406 — Pure GoalContinuityNextActionKind on continuity next

## Outcome

CLI `goal continuity-next` logs pure
`format_goal_continuity_next_action_kind_line` with structured
`action = as_str` for the projected next-action kind.

## Motivation

WP-401/403 wired continuity status/mode/step kinds on next-action projection.
The projected action kind itself still lacked a pure kind diagnostic.

## Acceptance

1. Add pure `format_goal_continuity_next_action_kind_line` using `action.as_str()`.
2. `continuity_next` logs pure action-kind line + `action = as_str` after projection.
3. Unit test asserts stable token text.
4. JSON/human bodies unchanged.
5. Local gates and independent review pass.
