# WP-244 — Display for IntentCategory

## Outcome

`IntentCategory` implements `Display` via existing public `as_str`.

## Motivation

Router intent tokens already use `as_str`. Adding Display continues the domain
token train for human/observability surfaces.

## Acceptance

1. `impl Display for IntentCategory` delegates to `as_str`.
2. Unit test locks representative tokens including multi-word `code_gen`.
3. Local gates and independent review pass; no CI isolation train.
