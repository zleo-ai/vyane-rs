# WP-141 — isolate read-only process-harness workdir fixture in CI

## Outcome

Run `read_only_explicit_workdir_is_the_fake_harness_cwd` in a fresh CI process
for normal and coverage jobs.

Production process-harness workdir pinning behavior does not change.

## Motivation

Under instrumented suite load this process-lane AgentRun fixture can exceed its
terminal status budget with only the daemon listen line (PR #94 coverage). The
body is stable alone; neighboring process-lane fixtures already use process
isolation for the same suite-handoff class.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
