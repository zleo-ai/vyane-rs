# WP-381 — Wire pure WorkflowControlError lines on MCP mapping

## Outcome

MCP `map_submit_error` / `map_control_error` log pure
`format_workflow_control_error_kind_line` for the mapped MCP control error after
logging the client/submit kind lines.

## Motivation

WP-312 published pure MCP `WorkflowControlError` lines. Mapping already logged
submit/control-client kinds; the mapped MCP surface stayed half-landed.

## Acceptance

1. After mapping submit errors, log pure MCP workflow-control line + `error = mapped.as_str`.
2. After mapping control-client errors, log pure MCP workflow-control line + `error = mapped.as_str`.
3. `format_workflow_control_error_kind_line` drops `#[allow(dead_code)]`.
4. Local gates and independent review pass.
