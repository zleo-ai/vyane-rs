# WP-131 — isolate graceful-stop restart settlement fixture in CI

## Outcome

Run
`graceful_stop_cancels_active_native_wire_and_restart_settles_without_replay` in a
fresh CI process for normal and coverage jobs, completing the restart-class
process isolations begun in WP-128.

Production graceful-stop and restart no-replay guarantees do not change. The
fixture stays on the intentional current-thread Tokio runtime.

## Motivation

This fixture drives a long delayed MockServer wire plus daemon kill/restart
settlement. Neighboring restart fixtures already required process isolation
after suite-load non-terminal observations. It remained the long-delay restart
body still serialized with the rest of the CLI suite.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes; runtime flavor remains current-thread.
4. Local gates, review, PR CI, and exact merged-main CI pass.
