# WP-240 — CLI takeover decision and continuity signal arg Display

## Outcome

`GoalTakeoverDecisionArg` and `GoalContinuitySignalArg` implement `Display`
through domain `TakeoverDecision` / `GoalContinuitySignalKind` tokens.

## Motivation

WP-228/229 published domain tokens for these kinds. CLI clap enums still lack
Display alignment with domain snake_case surfaces.

## Acceptance

1. Both clap enums Display via domain `as_str`.
2. Unit tests lock tokens against domain (signal tokens snake_case).
3. Local gates and independent review pass; no CI isolation train.
