# WP-139 — isolate native in-process cancel fixture in CI

## Outcome

Run `native_agent_cancel_uses_the_exact_in_process_controller` in a fresh CI
process for normal and coverage jobs.

Production native cancel controller behavior does not change.

## Motivation

Multi-thread native AgentRun cancel fixtures share suite-handoff sensitivity
with the already isolated submit/failover/sandbox cases.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
