# WP-172 — isolate daemon-client submission-timeout fixture in CI

## Outcome

Run `daemon_client::tests::submission_timeout_is_outcome_unknown_with_no_http_status` in a fresh CI process for normal and coverage jobs.

Production daemon-client submission timeout handling does not change.

## Motivation

This multi_thread Wiremock unit fixture shares suite-handoff sensitivity with
neighboring CLI multi_thread unit isolations.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` on the `vyane` binary.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
