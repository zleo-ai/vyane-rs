# WP-163 — isolate task-list ordering fixture in CI

## Outcome

Run `task_list_orders_recent_first_and_json_parses` in a fresh CI process for
normal and coverage jobs.

Production task-list ordering and JSON parse behavior does not change.

## Motivation

This multi_thread detach fixture lists recent tasks first and parses JSON.
Neighboring multi_thread detach fixtures already required process isolation
under suite/GHA load.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
