# WP-155 — isolate detach fingerprint-cancel fixture in CI

## Outcome

Run `fingerprint_cancel_does_not_depend_on_ps_or_path` in a fresh CI process
for normal and coverage jobs.

Production fingerprint-based cancel behavior does not change.

## Motivation

This multi_thread detach cancel fixture is the second cancel-class neighbor of
`cancel_finalizes_cancelled_and_kills_group` and shares suite-handoff sensitivity
with other multi_thread detach control-plane cases.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
