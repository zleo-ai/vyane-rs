# WP-125 — isolate native command-permission sandbox fixture in CI

## Outcome

Run `native_command_permission_reaches_the_real_resident_sandbox` in a fresh
CI process for normal and coverage jobs, alongside the process-agent,
mixed-lanes, and native-writable isolations.

Production native command-permission sandbox behavior does not change.

## Failure evidence

Main coverage attempt 1 on `7a0f0b9` (WP-124) failed
`native_command_permission_reaches_the_real_resident_sandbox` with
`AgentRun status request exceeded its polling budget` and only the daemon
listen line. Ubuntu/macOS non-coverage jobs and a failed-job re-run passed.
The body is stable alone; suite handoff under llvm-cov can leave status HTTP
unresponsive after earlier multi-thread daemon fixtures.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
