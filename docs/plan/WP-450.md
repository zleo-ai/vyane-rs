# WP-450 — Pure GoalNextReasonCode on REST continuity-next success

## Outcome

REST `GET /v1/goals/{id}/continuity-next` logs pure
`format_goal_next_reason_code_line` for the projected allowlisted
`GoalNextReasonCode` on success.

## Motivation

WP-447–449 cover REST accepted signals, action kind, and optional command.
`GoalNextActionView.reason_code` is a closed allowlisted taxonomy already on the
success body without a pure kind diagnostic (free-form reason text is never
logged).

## Acceptance

1. Add pure `format_goal_next_reason_code_line` over `GoalNextReasonCode::as_str`.
2. REST success path logs pure line + `reason_code = as_str`.
3. Unit tests lock representative closed reason tokens.
4. JSON response body and status mapping unchanged (no free-form reason text).
5. Local gates and independent review pass.
