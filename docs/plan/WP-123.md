# WP-123 — isolate native writable-command-root fixture in CI

## Outcome

Run `native_writable_command_root_reaches_the_real_resident_sandbox` in a fresh
CI process for normal and coverage jobs, alongside the process-agent and
mixed-lanes isolations.

Production native sandbox behavior does not change.

## Failure evidence

PR #70 coverage run left
`native_writable_command_root_reaches_the_real_resident_sandbox` failing with
`AgentRun submission did not return a response: total retry budget expired`
and only the daemon listen line while other checks passed. The body is stable
alone; suite handoff under llvm-cov can starve the first submit accept.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
