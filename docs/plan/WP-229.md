# WP-229 — Domain Display for continuity mode/status/step and takeover decision

## Outcome

Remaining continuity and takeover domain enums expose public `as_str` / `Display`
tokens matching serde snake_case (or explicit decision tokens).

## Motivation

WP-228 finished signal/criterion/pursuit tokens. Continuity mode, status, step
status, and takeover decision still lack domain tokens for human/CLI surfaces.

## Acceptance

1. `GoalContinuityMode`, `GoalContinuityStatus`, `GoalContinuityStepStatus`, and
   `TakeoverDecision` have public `as_str` + `Display`.
2. Unit tests lock the shipped tokens.
3. Local gates and independent review pass; no CI isolation train.
