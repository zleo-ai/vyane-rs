# WP-176 — isolate daemon-workflow concurrent-creates fixture in CI

## Outcome

Run `daemon_workflow::tests::concurrent_identical_creates_have_one_creator_and_one_idempotent_reader` in a fresh CI process for normal and coverage jobs.

Production daemon-workflow concurrent create behavior does not change.

## Motivation

This multi_thread unit fixture shares suite-handoff sensitivity with
neighboring CLI multi_thread unit isolations.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` on the `vyane` binary.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
