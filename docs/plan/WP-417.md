# WP-417 — Pure Protocol on native target freeze

## Outcome

Native target freeze logs pure `format_protocol_line` with structured
`protocol = as_str` for the bound target's closed protocol.

## Motivation

WP-413 added Protocol tokens and check-path pure lines. The native AgentRun
freeze path still mapped protocol into durable snapshots without a pure kind
diagnostic.

## Acceptance

1. `target_snapshot` logs pure protocol line + `protocol = as_str` for the bound target protocol.
2. Snapshot mapping and admission unchanged.
3. Reuses existing pure `format_protocol_line` (WP-413).
4. Local gates and independent review pass.
