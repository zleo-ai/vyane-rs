# WP-144 — isolate process-group cancel settlement fixture in CI

## Outcome

Run `cancel_stops_the_exact_process_group_and_settles_cancelled` in a fresh CI
process for normal and coverage jobs.

Production process-group cancel settlement behavior does not change.

## Motivation

This process-lane AgentRun cancel fixture shares suite-handoff sensitivity with
the already isolated process success and exit-zero fixtures. Under suite load
status polling can stall while the body remains stable alone.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
