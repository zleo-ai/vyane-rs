# WP-151 — isolate resident native post-tool no-failover unit fixture in CI

## Outcome

Run
`native_agent::tests::resident_native_run_never_fails_over_after_tool_activity`
in a fresh CI process for normal and coverage jobs.

Production post-tool no-failover behavior does not change.

## Motivation

This multi_thread multi-mock resident native unit fixture completes the
pre-tool failover / chain-drift / post-tool no-failover trio already partially
process-isolated under suite/GHA load.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
