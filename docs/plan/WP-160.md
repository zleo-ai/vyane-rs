# WP-160 — isolate detached direct-HTTP write reject fixture in CI

## Outcome

Run `detached_direct_http_write_is_rejected_before_task_or_process` in a fresh CI
process for normal and coverage jobs.

Production detached direct-HTTP write reject behavior does not change.

## Motivation

This multi_thread detach fixture rejects a direct HTTP write before task or
process creation. Neighboring multi_thread detach fixtures already required
process isolation under suite/GHA load.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
