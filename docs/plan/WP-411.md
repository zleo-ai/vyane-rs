# WP-411 — Pure Sandbox on CLI pursue

## Outcome

CLI `goal pursue` logs pure `format_sandbox_line` with structured
`sandbox = as_str` for the frozen pursuit sandbox before the pursuer runs.

## Motivation

Pursuit freezes a closed `Sandbox` on the shared segment runtime. Takeover
sandbox pure lines landed on queue (WP-410); CLI pursue still lacked a pure
sandbox kind diagnostic for the core sandbox axis.

## Acceptance

1. Add pure `format_sandbox_line` using `sandbox.as_str()` (kebab-case domain tokens).
2. `pursue` logs pure sandbox line + `sandbox = as_str` when constructing the runtime.
3. Unit test asserts stable token text (`read-only` / `write` / `full`).
4. Pursuit behavior and settle bodies unchanged.
5. Local gates and independent review pass.
