# WP-150 — isolate resident native chain-drift unit fixture in CI

## Outcome

Run
`native_agent::tests::resident_native_run_rejects_frozen_chain_drift_before_wire`
in a fresh CI process for normal and coverage jobs.

Production frozen chain-drift rejection does not change.

## Motivation

This multi_thread multi-mock resident native unit fixture is the next neighbor
of the isolated pre-tool failover case and shares the same suite-handoff class.

## Acceptance

1. Normal and coverage jobs skip the fixture in the serialized CLI suite.
2. Both jobs re-run it alone via `--exact` in a fresh process.
3. No production code or assertion changes.
4. Local gates, review, PR CI, and exact merged-main CI pass.
