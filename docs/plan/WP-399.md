# WP-399 — Pure PursuitStatus on CLI pursue settle

## Outcome

CLI `goal pursue` settlement logs pure `format_pursuit_status_line` with
structured `status = as_str` on both JSON and human response paths.

## Motivation

WP-362 wired pure pursuit status on daemon goal settle. The direct CLI pursue
path still settled without a pure kind diagnostic (human multi-line body only).

## Acceptance

1. After a successful pursue, log pure pursuit-status line + `status = as_str`.
2. JSON and human response bodies remain unchanged.
3. Local gates and independent review pass.
